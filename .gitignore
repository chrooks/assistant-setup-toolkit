.codex/
.agents/
.claude/
prompts/
.DS_Store
.mcp.json
artifacts/
node_modules/
dist/
.setup-cache/
tests/.tmp/
feature_requests/
.table-exports/
.diagram-exports/

# Personal self-profile: local-only, generated from "Chris's Brain" via /sync-self.
canonical/PROFILE.md

# Filled-in config: only the *.example.* templates under canonical/config/ are
# tracked. The real files hold personal absolute paths / secrets — keep local.
canonical/knowledge-config.json
secrets-pushover.env

# Machine rules are local-only (ADR-0003) — they describe a real box; never commit them.
canonical/rules/machines/*.md

# Setup Wizard run logs (see reporter.ts) — local diagnostics, not source.
.setup/
